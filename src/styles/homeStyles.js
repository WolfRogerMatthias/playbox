export const homeStyles = (theme) => ({
    container: {
        background: theme.palette.background.default,
        height: '100vh',
        width: '100vw',
        margin: 0,
        padding: 0,
    }
})